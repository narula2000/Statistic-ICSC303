1 + 1
1:10
# This is for a comment.
# You can run R command by Ctrl+Enter.

# We can assign data to a variable.
# a is a data set 2,5,6,9,8.
a <- c(2, 5, 6, 9, 8)
a
mean(a)
a^2
sqrt(a)
sd(a)
var(a)
#We can choose a data point from a vector.
a[2]
a[2:4]
#() is for function.
#[] is for subsetting data.

#I recommend you to use "<-" instead "=" for assignment.
a <- c(2, 3, 5, 9, -40)
a
mean(a)
sd(a)
var(a)
plot(a)
